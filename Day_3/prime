#include<stdio.h>
int main(){
    int n,i,found=0;
    printf("enter a no");
    scanf("%d",&n);
    for(i=2;i<n;i++){
        if(n%i==0){
            found=1;
            break;
        }
    }
    if(found==0){
    printf("%d is a prime number",n);
    }

    else if(found==1){
        printf("%d is not a prime number",n);
    }
}
