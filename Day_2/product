#include<stdio.h>
int main(){
    int n,d,product=1;
    printf("enter a no");
    scanf("%d",&n);
    while(n>0){
        d=n%10;
        product*=d;
        n=n/10;
    }
    printf("the product of the digit is %d",product);
}
